extraction:
  cpp:
    prepare:
      packages:
        - build-essential
        - linux-headers-${uname -r}
    after_prepare:
      - export GNU_MAKE=make
    index:
      build_command:
        - $GNU_MAKE

